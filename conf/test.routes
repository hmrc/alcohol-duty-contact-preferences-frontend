# Add all the application routes to the app.routes file
GET     /clear-all                                            testOnly.controllers.TestOnlyController.clearAllData()
GET     /create-and-show-user-answers                         testOnly.controllers.TestOnlyController.createAndShowUserAnswers()

GET     /landing-page                                         testOnly.controllers.TestOnlyController.testOnlyLandingPage()
GET     /create-and-fill-user-answers/:verified/:locked       testOnly.controllers.TestOnlyController.createAndFillUserAnswers(verified: Boolean, locked: Boolean)
